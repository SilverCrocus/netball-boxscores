Update all project documentation after recent changes:

1. Regenerate CODEBASE.md by running `python3 scripts/generate-codebase-md.py`
2. Update CLAUDE.md if file roles, DB schema, architecture, shared components, or gotchas changed
3. Update MEMORY.md (at ~/.claude/projects/-Users-mortimerme-Documents-GitHub-personal-netball-boxscores/memory/MEMORY.md) with any new patterns, bug fixes, or non-obvious decisions

Only update files that actually need changes. Don't add entries for trivial edits.
